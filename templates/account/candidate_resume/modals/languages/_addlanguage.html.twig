<a class="button-text-1" href="" data-toggle="modal" data-target="#addLanguage" >Ajouter</a>
<div class="modal fade" id="addLanguage" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content p-2">
            <form class="form-style-5" method="post" action="{{ path('app_profil_add_language') }}" autocomplete="on">
                <div class="modal-header">
                    <h6>Ajout d'une langue</h6>
                    <a type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </a>
                </div>
                <div class="modal-body p-3">
                    <div class="row g-4 m-2">
                        <div class="form-group col-md-6">
                            <label>Langue</label>
                            <select id="inputLangue" class="form-select" name="languageName" required>
                                {{ include("./account/candidate_resume/modals/languages/language_options.html.twig") }}
                            </select>

                        </div>
                        <div class="form-group ">
                            <label>Niveau parler</label>
                            <select class="form-select" name="languagespeak" required>
                                <option value="Initiation">Initiation</option>
                                <option value="Courant">Courant</option>
                                <option value="Intermediaire">Intermediaire</option>
                                <option value="Professionnel">Professionnel</option>
                                <option value="Expert">Expert</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Niveau écrire</label>
                            <select class="form-select" name="languagewrite" required>
                                <option value="Initiation">Initiation</option>
                                <option value="Courant">Courant</option>
                                <option value="Intermediaire">Intermediaire</option>
                                <option value="Professionnel">Professionnel</option>
                                <option value="Expert">Expert</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <a type="button" class="button button-radius text-bg-danger" data-dismiss="modal">Fermer</a>
                    <input type="hidden" name="token" value="{{ csrf_token('add-language') }}"/>
                    <button type="submit" class="button button-xs button-radius button-outline-dark-2">Enregistrer</button>
                </div>
            </form>
        </div>
    </div>
</div>